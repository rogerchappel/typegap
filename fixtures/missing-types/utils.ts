// Codebase missing return types and type annotations

export function greet(name) {
  return `Hello, ${name}`;
}

export function add(a, b) {
  return a + b;
}

export const double = (x) => x * 2;

export function processItems(items) {
  return items.map(item => item.name);
}
